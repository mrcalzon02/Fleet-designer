{
  "name": "fleet-designer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "diagnostics": "node scripts/runtime-diagnostics.mjs",
    "repair:electron": "node scripts/repair-electron.mjs",
    "smoke": "npm run diagnostics && npm run build",
    "desktop:launch": "npm run build && electron .",
    "desktop": "npm run repair:electron && npm run desktop:launch",
    "dist:win": "npm run repair:electron && npm run build && electron-builder --win portable",
    "dist:win:installer": "npm run repair:electron && npm run build && electron-builder --win nsis"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest",
    "lucide-react": "latest",
    "recharts": "latest"
  },
  "devDependencies": {
    "eslint": "latest",
    "@eslint/js": "latest",
    "eslint-plugin-react-hooks": "latest",
    "eslint-plugin-react-refresh": "latest",
    "globals": "latest",
    "electron": "38.4.0",
    "electron-builder": "26.0.12"
  },
  "build": {
    "appId": "com.orbitalworks.fleetdesigner",
    "productName": "Fleet Designer - Orbital Works",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "electron/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        "portable",
        "nsis"
      ],
      "artifactName": "Fleet-Designer-Orbital-Works-${version}-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "shortcutName": "Fleet Designer - Orbital Works"
    }
  }
}
